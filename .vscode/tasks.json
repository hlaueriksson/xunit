{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "dotnet build",
            "command": "dotnet build",
            "type": "shell",
            "group": "build",
            "problemMatcher": "$msCompile"
        }
    ]
}